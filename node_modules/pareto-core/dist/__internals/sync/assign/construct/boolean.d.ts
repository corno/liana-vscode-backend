import * as _pi from "../../../../interface";
export declare namespace from {
    const dictionary: <T>(dictionary: _pi.Dictionary<T>) => {
        is_empty: () => boolean;
    };
    const list: <T>(list: _pi.List<T>) => {
        is_empty: () => boolean;
        reduce: (initial_state: boolean, update_state: (value: T, current: boolean) => boolean) => boolean;
    };
    const optional: <T>(optional: _pi.Optional_Value<T>) => {
        is_set: () => boolean;
    };
}
