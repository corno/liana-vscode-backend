import * as _pi from "./interface";
export declare namespace acyclic {
    const not_set: <T>() => _pi.lookup.Acyclic<T>;
    const from_resolved_dictionary: <T>(dict: _pi.Dictionary<T>) => _pi.lookup.Acyclic<T>;
}
export declare namespace cyclic {
    const not_set: <T>() => _pi.lookup.Cyclic<T>;
}
export declare namespace stack {
    const empty: <T>() => _pi.lookup.Stack<T>;
    const push: <T>(stack: _pi.lookup.Stack<T>, item: _pi.lookup.Acyclic<T>) => _pi.lookup.Stack<T>;
}
